require_relative "../classes/report_generator.rb"

describe ReportGenerator do
  let (:report_generator) { ReportGenerator.new }

  xit "should export the formatted data to output.csv" do
    pending "To do"
  end

  xit "should export the invalid data to report.txt" do
    pending "To do"
  end

  xit "should export the number of invalid records to report.txt" do
    pending "To do"
  end

  xit "should export the number of valid records to report.txt" do
    pending "To do"
  end
end